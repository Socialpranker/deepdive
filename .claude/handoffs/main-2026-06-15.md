# main — 2026-06-15

**Дата:** 2026-06-15 | **Статус:** Phase 7 замержена; следующий этап не начат
**Цель:** Закрыта Phase 7 (Refresh targets). Дальше — `synthesize()` block-render (самый важный незакрытый кусок pipeline).

## Продолжить работу

**Следующий шаг — реальный `synthesize()` (Phase 6 block-render).** Сейчас это scaffold (`runner/orchestrator.py:284-298`): пишет только `# {question}` + одну плейсхолдер-строку TL;DR + один counter-arg. Реальных блоков отчёта (M/N/C/A, open-questions, next-research, sources, metadata) НЕ генерирует.

Почему это первоочередное: оно разблокирует сразу три вещи —
1. реальный отчёт `<date>_<genre>.md` (сейчас пустой);
2. наполнение `<!-- TODO -->`-маркеров в Phase 7 refresh реальными данными (entities из M2/M5, numbers из N1-N8, topics) — Phase 7 их специально оставила ждать именно block-render;
3. осмысленный verify цитат (Phase 6.5 — сейчас проверять нечего).

Спека блоков: `references/blocks/*.md` (по типам M/N/C/A + close.md с Z11). Workflow: `references/workflow.md` Phase 6 (Synthesis) требует полный отчёт со всеми блоками жанра.

**Второй по важности (дешевле) — `reframe()` (Phase 1), тоже scaffold** (`orchestrator.py:117`): возвращает пустые гипотезы `H{i}: hypothesis [...]`. Из-за этого в реальном refresh-выводе у гипотез пустой текст. Реальный reframe → осмысленные H1-H4 из вопроса.

**Паттерн фазы (устоялся за 4 фазы — scoring/capabilities/verify/refresh):** чистый модуль `runner/<phase>.py` (pure-логика + render, без сети) + тонкий метод `Orchestrator.<phase>(s)` в run(). Тесты оффлайн на DryRunProvider. brainstorming → дизайн-док (`docs/superpowers/specs/`) → writing-plans (`docs/superpowers/plans/`) → subagent-driven-development (имплементеры sonnet, каждая задача spec+quality ревью) → финальный code-review до мерджа.

**Уроки (память проекта `memory/`):**
- `anthropic-structured-output-schema-limits` — если будет новая JSON-схема для LLM: без min/max/minLength/maxLength/minItems/maxItems и `additionalProperties: false` на каждом object-узле, иначе HTTP 400. ОБЯЗАТЕЛЬНО расширить guard-тест `test_signals_schema_is_structured_output_safe` на новую схему.
- `phase65-verify` — если фаза ходит в сеть/subprocess: сделать сеть opt-in флагом, иначе все run()-тесты станут сетевыми.

**Окружение:** в шелле только `python3` (НЕ `python` — `command not found`). Дефолтный `python3 -m pytest` теперь ЗЕЛЁНЫЙ (161 passed, 6 deselected); live-тесты деселектятся через `addopts -m "not live"` в pytest.ini, запуск live — явным `-m live`. CLI-прогон: `python3 runner/orchestrator.py "q" --provider dryrun --out research` ИЛИ `PYTHONPATH=. python3 -m runner.orchestrator ...` — оба работают.

**НЕ делать без запроса:** push/PR/мердж — outward-facing, по явному запросу Ивана (он подтверждал каждый раз). origin/main = локальный main (синхронизированы).

## Сделано (эта сессия)

- **Phase 7 Refresh РЕАЛИЗОВАНА И ЗАМЕРЖЕНА** (PR #5 → main, merge-commit `9f03880`). `runner/refresh.py` (5 pure-функций: extract_hypotheses/entities/numbers/carry_forward + render_refresh_targets), `Orchestrator.refresh(s)` после verify() в run(), `tests/test_refresh.py` (25 тестов). Пишет `<slug>/refresh_targets.md` (шаблон Z11) из RunState. Гейт `if depth=="shallow": return`.
- **2 pre-existing инфра-фикса** (попутно, вошли в PR #5): `pytest.ini` addopts `-m "not live"` (дефолтный pytest стал зелёным); `runner/providers.py` try/except-импорт hypothesis_ids (скриптовый запуск orchestrator больше не падает ModuleNotFoundError).
- Дизайн+план Phase 7 (`docs/superpowers/specs/2026-06-15-phase7-refresh-design.md`, `docs/superpowers/plans/2026-06-15-phase7-refresh.md`).

## Осталось (следующие этапы, НЕ Phase 7)

- [ ] **`synthesize()` block-render (Phase 6)** — реальные блоки M/N/C/A вместо scaffold. ПЕРВООЧЕРЕДНОЕ.
- [ ] **`reframe()` (Phase 1)** — реальные гипотезы вместо пустых плейсхолдеров.
- [ ] Block-rendering M/N/C/A типов (зависит от synthesize).
- [ ] Заполнить `<!-- TODO -->`-маркеры в refresh.py реальными данными (зависит от block-render).
- [ ] Update-mode pipeline — реальный дельта-прогон `update <slug>`, читающий refresh_targets.md (Phase 7 генерирует targets, но потребителя ещё нет).
- [ ] Phase 2 genre — интерактивный выбор блоков (сейчас regex без диалога).

## Решения (эта сессия)

- **Источник данных Phase 7 = RunState, не парсинг отчёта.** Потому что synthesize() scaffold — блоков M2/N1/A4 в отчёте физически нет. Извлекаем из s.hypotheses/sources/triangulation/deviations.md. Чего нет — машинно-видимый `<!-- TODO -->`, не выдумываем.
- **Сети/subprocess в Phase 7 нет** → run()-тесты остаются оффлайн by design (урок verify() соблюдён без флага).
- **2 инфра-фикса оставлены в PR #5** (не выносил в отдельный PR) — маленькие (+6 строк), делают suite зелёным, по решению Ивана.
- **merge-commit (не squash)** — сохранить 12 атомарных TDD-коммитов фазы для аудита.
